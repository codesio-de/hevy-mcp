{
  "name": "hevy-mcp",
  "version": "0.2.0",
  "description": "Hevy MCP Server with OAuth, running on Cloudflare Workers",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "agents": "^0.16.0",
    "zod": "^4.4.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260613.1",
    "typescript": "^5.7.0",
    "wrangler": "^4"
  }
}
